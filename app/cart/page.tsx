import { CartPage } from "@/components/cart-page";

export const metadata = {
  title: "Cart | RZ Dental",
  description: "Review your selected products and submit your dental booking request.",
};

export default function Page() {
  return <CartPage />;
}
